services:
  - type: web
    name: astrologer-crm-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        value: astrologer_crm_secret_key_2024
      - key: PORT
        value: 5000
